<Window x:Class="WpfBaseLesson.StyleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfBaseLesson"
        mc:Ignorable="d"
        Title="样式" Height="450" Width="800">
    <!--
        将样式封装成资源
    -->
    <Window.Resources> <!--只在本页面中生效-->

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Content="注册" Style="{StaticResource buttonStyle}"></Button>
        <Button Content="登录" Style="{StaticResource buttonStyle}" Grid.Row="1"></Button>
        <Button Content="其他" Grid.Row="2"></Button>
    </Grid>
</Window>
